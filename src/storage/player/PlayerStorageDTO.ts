/* Essa sigla DTO é uma convenção dessas tipagens, objetos que utilizamos para levar de um lugar para outro, por exemplo, da aplicação para uma API, da aplicação para o armazenamento */

export type PlayerStorageDTO = {
  name: string;
  team: string;
}